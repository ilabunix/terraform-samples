The root cause was duplicate resource naming across East and West regions, which caused Terraform state conflicts and stale lifecycle errors in Grafana.

To resolve this, Mark is clearing the existing alert resources via the pipeline and then recreating them cleanly from a fresh state.

His view is that re-running the cleanup step serves as the backout plan. However, this approach does not restore the prior configuration — it simply re-cleans and rebuilds.